
{{#paper-data-table-column 
	sortChanged=onSortChanged
    sortProp=sortProp
    currentProperties=currentProperties
    filterProp=filterProp}}
    {{yield}}
{{/paper-data-table-column }}
{{!-- {{paper-filter-table-dialog}} --}}
{{paper-input placeholder="search" value=name onChange=(action (mut name)) icon="filter list"}}
